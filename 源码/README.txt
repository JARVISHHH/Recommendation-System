Note：exe文件在压缩包中，使用前请解压文件夹中的data.rar，并将exe文件和数据文件放在同一个目录下。
1.使用的文件
	该程序中所有的要使用的文件和生成的文件都在同一个目录下。

2.代码运行过程
（1）代码会询问是否需要加载保存的模型，输入'y'加载模型，其他任意输入都不加载。
	注：如果当前目录下没有保存的模型，就会跳过这个询问，重新训练。
（2）代码会询问是否进入调参模式，输入'y'进行调参，其他任意输入都不进行调参。
	注：因为调参模式会花费13-14小时，极度不推荐使用这个模式。

3.预测的结果
预测的所有结果分数都设置为了10的倍数。